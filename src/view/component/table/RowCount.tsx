import React from 'react';
import * as Styled from 'view/component/table/Styled.RowCount';

const RowCount = () => {
  return <Styled.RowCount>Row Count</Styled.RowCount>;
};

export default RowCount;

import React from 'react';
import Layout from 'layout/Layout';
import * as Styled from 'App.styled';

const App = () => {
  return (
    <Styled.App>
      <Layout />
    </Styled.App>
  );
};

export default App;
